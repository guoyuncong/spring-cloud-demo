server:
  port: 8080
spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      config:
        server-addr: 101.35.111.41:8848
        namespace: dev
        group: DEFAULT_GROUP
        prefix: example
        file-extension: properties
